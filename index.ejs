<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="style.css">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">
    <style>
        .movie-list {
    list-style: none;
    display: flex;
    flex-wrap: wrap;
    justify-content: space-around;
    padding: 0;
}

.movie-list .movie-card {
    width: 250px;
    margin: 20px;
    padding: 15px;
    border: 1px solid #ccc;
    border-radius: 5px;
    box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
    background-color: #fff;
    text-align: center;
}

    .movie-image {
    width: 100%;
    height: auto;
    border-radius: 5px;
}

.movie-title {
    margin: 10px 0;
    font-size: 1.2em;
}

.movie-genre, 
.movie-rating {
    margin: 5px 0;
    font-size: 1em;
    color: #666;
}

    </style>
</head>
<body>
    <h1>movies</h1>
    <ul class="movie-list">
        <% movie.map((x) => { %>
            <li class="movie-card">
                <img src="<%= x.image %>" alt="<%= x.title %>" class="movie-image">
                <div class="movie-info">
                    <h2 class="movie-title"><%= x.title %></h2>
                    <p class="movie-genre">Genre: <%= x.genre %></p>
                    <p class="movie-year">Relase date: <%= x.year %></p>
                    <p class="movie-rating">Rating: <%= x.rating %></p>
                </div>
            </li>
        <% }) %>
    </ul>
    
<!-- <% console.log(movie) %> -->
</body>
</html>